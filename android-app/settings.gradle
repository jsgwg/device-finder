pluginManagement {
    repositories { google(); mavenCentral(); gradlePluginPortal() }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 极光推送 SDK 官方 Maven 源
        maven { url = uri("https://mvn.jiguang.cn/nexus/content/repositories/releases/") }
    }
}
rootProject.name = "DeviceFinder"
include(":app")
