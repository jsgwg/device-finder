# AndroidX（androidx.appcompat / work / core-ktx 均需要）
android.useAndroidX=true
# 项目未使用旧 support 库，无需 Jetifier
android.enableJetifier=false
# Kotlin 代码风格
kotlin.code.style=official
# JVM 内存
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
